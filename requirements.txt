# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.26.0
scipy>=1.12.0
mutagen>=1.46.0

# Data Handling
pandas>=2.1.0
matplotlib>=3.8.0
plotly>=5.18.0

# Analysis & ML
scikit-learn>=1.4.0

# Stem Separation & Audio Processing
demucs>=4.0.0
torch>=2.0.0  # Required by Demucs

# Optional: Advanced mood classification (requires special dependencies)
# essentia>=2.1.0  # For TensorFlow-based mood models (optional, with fallback)

# Optional: S-KEY deep-learning key detection (much better than the chroma template
# for electronic music). When installed, mood_engine uses it automatically and reports
# a real confidence; otherwise it falls back to the built-in chroma method.
# Not on PyPI yet — install the package from GitHub, then these deps:
#   git clone https://github.com/deezer/skey && pip install --no-deps ./skey
# torchaudio>=2.7.0  # torch itself is already required above (Demucs)
# einops>=0.8.0
# nnAudio==0.3.3

# Web Dashboard (optional)
streamlit>=1.28.0

# Development
pytest>=7.4.0
pytest-cov>=4.1.0
ruff>=0.1.0

# API & Utilities
openai>=1.4.0
python-dotenv>=1.0.0
tabulate>=0.9.0
tqdm>=4.65.0
