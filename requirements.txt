# Dependencies are defined in pyproject.toml (single source of truth).
#
# Install the project in editable mode with the dev + dashboard extras:
#     pip install -r requirements.txt
# which resolves to the line below.
-e .[dev,dashboard]

# --- Optional extras (install explicitly when you need them) ------------------
#
# S-KEY deep-learning key detection (much better than the chroma template for
# electronic music; mood_engine uses it automatically when importable, else falls
# back to chroma). Install its deps via the extra, then the package from GitHub
# (S-KEY is not on PyPI yet):
#     pip install -e ".[skey]"
#     git clone https://github.com/deezer/skey && pip install --no-deps ./skey
#
# Essentia-based mood models (optional, with a rule-based fallback):
#     pip install -e ".[essentia]"
#
# MelBand / BS Roformer stem separation via audio-separator (cleaner kick/bass/synth
# separation on techno than HTDemucs). Pass a Roformer model name to StemSeparator
# (backend auto-detected) or backend="audio-separator":
#     pip install -e ".[roformer]"
